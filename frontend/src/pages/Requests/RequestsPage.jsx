// First, make sure all imports are at the top
import React from 'react';  // Correct import order

// Simple functional component
const RequestsPage = () => {
  return (
    <div>
      <h1 className="text-2xl font-bold">Requests Page</h1>
      {/* You can add the rest of your component's logic here */}
    </div>
  );
};

export default RequestsPage;  // Ensure the component is exported

